/**
 * Driver: <First Name> <Last Name>
 * Navigator: <First Name> <Last Name>
 **/

// header files
#include <stdio.h>

// main program
int main()
{
    // declare variable
    int numbers[2][2];

    // initialize variable
    for (int i = 0; i < 2; i++)
    {
        for (int j = 0; j < 2; j++)
        {
            scanf("%d", &numbers[i][j]);
        }
    }

    // TODO: print the value at row 0 column 1
    

    // end program
    return 0;
}
